extern const size_t gObjectsOffset;
extern const size_t gNamesOffset;

extern int VTABLE_SIZE;

// Booleans
extern bool IsAiDeactivated;
extern bool isOffice01AiDeactivated;
extern bool IsOfficeGeneralAiDeactivated;
extern bool nolightblocker;
extern bool InitiateTempFullBright;
extern bool ShowOutputLights;
extern bool ShowOutputLightBlock;
extern bool isKillBlockerActive;
extern bool revealsize;

// HACK CONFIG
extern bool lightmods;

// SAVEFILE GENERATION
extern bool SaveManageBool_Active_SaveEdits;

// ENABLE THE BOOL ABOVE TO UNLOCK THE GENERATIONS BELOW!

// UNLOCK ALL WITHOUT PRIZES
extern bool SaveManageBool_UnlockAll_NoPrizes;
// UNLOCK EVERYTHING
extern bool SaveManageBool_FullUnlock;

// Select what you want to unlock, just keep the above ones to false!
// Unlock Levels
extern bool SaveManageBool_UnlockLevels;
// Unlock Glitches
extern bool SaveManageBool_UnlockGlitches;
// Unlock Coins
extern bool SaveManageBool_UnlockCoins;
// Unlock Prizes
extern bool SaveManageBool_UnlockPrizes;

// Spotlight Configurator
extern float Spotlight_Multiplicator;
extern float Spotlight_Addiction;
extern float spotlight_brightness_divisor;
extern float Spotlight_brightness_subtractor;
extern float Spotlight_intensity;

// GAMMA CONFIG
extern float Engine_Gamma;

// General Vars
extern float Max_float;
extern int max_int;
extern int counter;
extern unsigned char ColorVar;
extern unsigned char zero;